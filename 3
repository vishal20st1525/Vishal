import textwrap
import textwrap

def wrap(string, max_width):
    # textwrap.fill wraps the string and returns a single string with newlines
    return textwrap.fill(string, max_width)
if __name__ == '__main__':
